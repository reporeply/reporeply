name: RepoReply Daily Scan

on:
  schedule:
    - cron: "0 2 * * *" # 02:00 UTC daily
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Call daily cron endpoint
        run: |
          curl -X POST https://esp32dev.xyz/cron/daily